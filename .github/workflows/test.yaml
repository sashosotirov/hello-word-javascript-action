
# triger workflow events
on: 
  push:
    
jobs:
  update-files:
    runs-on: ubuntu-latest  
    name: Update file content
    steps:
        #This action checks-out your repository under $GITHUB_WORKSPACE, so your workflow can access it.
      - name: Checkout
        uses: actions/checkout@v3               
        
      - name: update files
        id: update
        uses: sashosotirov/hw4@main
        with:
          remote-repo: kubernetes_wildfly
          remote-owner: sashosotirov
          remote-file: README.md
          current-file: README.md
          remoteToken: ${{ secrets.GH_PAT }}
          currentToken: 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 
          
      